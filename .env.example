# Copy to .env and fill in your key from https://dashboard.exa.ai
# .env is gitignored — never commit a real key.
EXA_API_KEY=YOUR_API_KEY

# Optional: override the API base URL (defaults to https://api.exa.ai)
# EXA_BASE_URL=https://api.exa.ai
